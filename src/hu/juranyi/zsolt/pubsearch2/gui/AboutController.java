package hu.juranyi.zsolt.pubsearch2.gui;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.event.ActionEvent;
import javafx.fxml.Initializable;

public class AboutController
        implements Initializable {

    // Handler for Hyperlink[Hyperlink[id=null, styleClass=hyperlink]] onAction
    public void blogen(ActionEvent event) {
        // handle the event here
    }

    // Handler for Hyperlink[Hyperlink[id=null, styleClass=hyperlink]] onAction
    public void bloghu(ActionEvent event) {
        // handle the event here
    }

    // Handler for Hyperlink[Hyperlink[id=null, styleClass=hyperlink]] onAction
    public void mail(ActionEvent event) {
        // handle the event here
    }

    // Handler for Hyperlink[Hyperlink[id=null, styleClass=hyperlink]] onAction
    public void sourceforge(ActionEvent event) {
        // handle the event here
    }

    @Override // This method is called by the FXMLLoader when initialization is complete
    public void initialize(URL fxmlFileLocation, ResourceBundle resources) {
        // initialize your logic here: all @FXML variables will have been injected
    }
}
